{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-local-only",
  "description": "Only Loomlight's local main window may call the versioned core envelope.",
  "local": true,
  "webviews": ["main"],
  "permissions": ["allow-loomlight-core"]
}
