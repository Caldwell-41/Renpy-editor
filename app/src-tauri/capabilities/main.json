{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-local-only",
  "description": "Only the local main window may call the versioned core envelope and guarded application close.",
  "local": true,
  "webviews": [
    "main"
  ],
  "permissions": [
    "allow-loomlight-core",
    "allow-application-close"
  ]
}
